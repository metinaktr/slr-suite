# v2.3.2 release preparation

Status: local preparation only. No tag, GitHub Release, new DOI or publication
date has been issued. Archived v2.3.1 is unchanged.

Evaluated code: `2e7cd2854c3aef9073e30ac9e813ba2fef97ddca`.
Release preparation changes metadata, documentation and evidence, not analytical
R source. The eventual release commit will differ from the benchmark commit;
code identity must be verified before tagging. Record the final commit and new
version-specific archive DOI only after publication/archiving is confirmed.
Preparation check: all 21 R files in `R/` and `scripts/` matched the benchmark
copy after normalizing only CRLF/LF line endings. Code metadata JSON parsed,
and the Git whitespace check passed. Final release validation is still pending.

| Records | Five-run time median (Q1–Q3), seconds | Approximate peak working set median (Q1–Q3), MiB |
|---|---|---|
| 20 | 19.046 (18.012–24.020) | 473.68 (473.04–474.24) |
| 208 | 31.064 (31.023–31.597) | 535.85 (535.64–536.00) |
| 500 | 52.708 (52.426–53.425) | 544.24 (533.06–546.04) |

All 15 runs and 135 module executions passed. Sixteen required processed files
per run were non-empty; thematic PNG signature, SVG paths and expected positive
flow counts (2, 39, 46) were verified. Source exports were unchanged. Approved
working copies removed only the leading UTF-8 BOM for the medium/large inputs.

Windows 11 Pro 10.0.26200, 64-bit; Intel Core i7-10510U, 8 logical processors;
17009872896 bytes of RAM (approximately 15.84 GiB); R 4.5.1. All 130 locked package
versions matched. Sequential fresh Rscript sessions; output directories reset;
no excluded warm-up and no explicit cache flushing. Approximate summed tracked
Rscript working set was sampled with a nominal 100-ms polling delay plus overhead.
Q1–Q3 uses R type-7 quantiles. Locale/package-build warnings occurred without failure.

PR #17 cross-platform validation passed on Ubuntu, Windows and macOS at its head
commit `e882b8fae993bdf87106b68018fa1135d5a8fb39`: 45 tests per platform, 80.77%
core coverage, unchanged 70% threshold. macOS installed XQuartz for Cairo SVG.
Evidence: https://github.com/metinaktr/slr-suite/actions/runs/34958580929

The repeated benchmark is Windows-only. It is not cross-platform timing evidence,
general scalability evidence or independent scientific classification validation.
Licensed inputs and record-level outputs are not included in the release preparation.

Before publication: validate this preparation, merge it, identify the final commit,
verify analytical source identity, approve the tag/release, archive permanently,
and synchronize DOI/date/commit in the manuscript and response documents.
